# Compute Travel Costs

Builds tract centroids and a straight-line distance matrix across Chicago tracts.

Used to provide tract centroid inputs for travel-time routing and downstream model tasks.

## Running

```bash
cd code && make
```

Main outputs: `output/tract_centroids.csv` and `output/distance_matrix_km.csv`.
