# Download Network Data

Downloads the Chicago OSM network extract and CTA GTFS feed used by `r5r`.

These are the base network inputs for travel-time calculations.

## Running

```bash
cd code && make
```

Main outputs: `output/chicago.osm.pbf` and `output/cta_gtfs.zip`.
