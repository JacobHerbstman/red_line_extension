# Download Census Tracts

Downloads 2010 census tract boundaries and tract-level ACS data used throughout the project.

Creates Chicago and Cook County tract files in both geospatial and flat formats.

## Running

```bash
cd code && make
```

Main outputs: `output/chicago_tracts_2010.gpkg` and `output/chicago_tracts_2010.csv`.
