# Download Commercial Floorspace

Downloads Cook County commercial valuation records and classifies them into commercial and multifamily groups.

Outputs PIN-level floorspace files used in tract aggregation.

## Running

```bash
cd code && make
```

Main outputs: `output/commercial_floorspace_pins.csv` and `output/multifamily_floorspace_pins.csv`.
