# Map Floorspace Shares

Computes tract-level residential and commercial floorspace shares from aggregated parcel totals.

Also writes map figures and the tract floorspace file used by gravity and inversion.

## Running

```bash
cd code && make
```

Main output: `output/tract_floorspace_for_model.csv` (plus map PDFs).
