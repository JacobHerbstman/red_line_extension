# Download Residential Floorspace

Downloads Cook County residential improvement records and keeps PIN-level floorspace fields.

Used with parcel coordinates in `create_floorspace_data`.

## Running

```bash
cd code && make
```

Main output: `output/residential_floorspace_pins.csv` (plus a diagnostics text file).
